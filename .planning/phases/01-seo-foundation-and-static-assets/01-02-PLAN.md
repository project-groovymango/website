---
phase: 01-seo-foundation-and-static-assets
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/main.jsx
  - src/data/content.js
  - src/components/ToolsMarquee.jsx
  - src/index.jsx
autonomous: false
requirements:
  - PERF-01
  - PERF-02
  - BUG-01
  - BUG-02

must_haves:
  truths:
    - "Fonts render using self-hosted WOFF2 files with no requests to fonts.googleapis.com or fonts.gstatic.com"
    - "Google Fonts preconnect and stylesheet links are removed from index.html"
    - "Planhat logo no longer appears in the logo grid or tools marquee"
    - "Banner link points to a real URL (not href='#')"
  artifacts:
    - path: "src/main.jsx"
      provides: "Fontsource CSS imports for DM Sans and DM Mono"
      contains: "@fontsource/dm-sans"
    - path: "src/data/content.js"
      provides: "LOGOS array without Planhat entry"
    - path: "src/components/ToolsMarquee.jsx"
      provides: "Marquee LOGOS array without Planhat entry"
    - path: "src/index.jsx"
      provides: "Banner with real link URL"
    - path: "index.html"
      provides: "Head without Google Fonts links"
  key_links:
    - from: "src/main.jsx"
      to: "node_modules/@fontsource/dm-sans"
      via: "CSS import"
      pattern: "import.*@fontsource/dm-sans"
    - from: "src/main.jsx"
      to: "node_modules/@fontsource/dm-mono"
      via: "CSS import"
      pattern: "import.*@fontsource/dm-mono"
---

<objective>
Migrate fonts from Google Fonts CDN to self-hosted fontsource packages, remove the Planhat logo from all logo grids, and fix the banner "click here" broken link.

Purpose: Eliminate render-blocking external font requests (improving LCP), remove a broken logo entry, and fix a link that currently goes nowhere.
Output: Fonts load from same-origin WOFF2 files, Planhat removed from data and components, banner link functional.
</objective>

<execution_context>
@C:/Users/amin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/amin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-seo-foundation-and-static-assets/01-CONTEXT.md
@.planning/phases/01-seo-foundation-and-static-assets/01-RESEARCH.md

<interfaces>
From src/main.jsx:
```javascript
import React from "react";
import ReactDOM from "react-dom/client";
import App from "./index";
```

From src/data/content.js (lines 1-2, 20):
```javascript
import planhat from "../assets/planhat-logo.svg";
// ...
{ src: planhat, alt: "Planhat", dot: false },  // line 20 — REMOVE
```

From src/components/ToolsMarquee.jsx (lines 2, 16):
```javascript
import planhat from "../assets/planhat-logo.svg";  // line 2 — REMOVE
// ...
{ src: planhat, alt: "Planhat" },  // line 16 — REMOVE
```

From src/index.jsx (lines 42-45):
```javascript
<div className="banner">
  Now released — free automation blueprints by a2labs{" "}
  <a href="#" target="_blank" rel="noreferrer">click here →</a>
</div>
```

From index.html (lines 9-11 — to be removed):
```html
<link rel="preconnect" href="https://fonts.googleapis.com" />
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
<link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600&family=DM+Mono:wght@400;500&display=swap" rel="stylesheet" />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install fontsource packages, add imports, and remove Google Fonts from index.html</name>
  <files>src/main.jsx, index.html</files>
  <action>
1. **Install fontsource packages** (PERF-01):
```bash
npm install @fontsource/dm-sans @fontsource/dm-mono
```
Verify the packages installed successfully and check their actual versions (research noted 5.2.6/5.2.7 but versions may differ).

2. **Add font imports to src/main.jsx** (PERF-01):
Add these imports at the TOP of the file, before the React import:
```javascript
import "@fontsource/dm-sans/400.css";
import "@fontsource/dm-sans/500.css";
import "@fontsource/dm-sans/600.css";
import "@fontsource/dm-mono/400.css";
import "@fontsource/dm-mono/500.css";
```
These replace the Google Fonts CDN. The font-family names remain `'DM Sans'` and `'DM Mono'` — no CSS changes needed in global.css.

3. **Remove Google Fonts links from index.html** (PERF-02):
Remove these three lines from `<head>`:
- `<link rel="preconnect" href="https://fonts.googleapis.com" />`
- `<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />`
- `<link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600&family=DM+Mono:wght@400;500&display=swap" rel="stylesheet" />`

Do NOT remove or modify any meta tags, JSON-LD, or other head content that Plan 01 may have added. Only remove the three Google Fonts lines.

Fontsource includes `font-display: swap` by default, satisfying the WOFF2 + swap requirement.
  </action>
  <verify>
    <automated>cd C:/Users/amin/WebstormProjects/website && npm run build && ls dist/assets/*.woff2 | head -5 && ! grep -i "googleapis" dist/index.html && echo "No Google Fonts references" && echo "WOFF2 files present"</automated>
  </verify>
  <done>npm build succeeds, WOFF2 font files appear in dist/assets/, no references to googleapis.com or gstatic.com remain in built index.html, fonts still render correctly (same font-family names)</done>
</task>

<task type="auto">
  <name>Task 2: Remove Planhat logo and fix banner link</name>
  <files>src/data/content.js, src/components/ToolsMarquee.jsx, src/index.jsx</files>
  <action>
1. **Remove Planhat from src/data/content.js** (BUG-02):
   - Remove the `import planhat from "../assets/planhat-logo.svg";` import (line 2)
   - Remove the `{ src: planhat, alt: "Planhat", dot: false }` entry from the LOGOS array (line 20)
   - Keep all other imports and array entries intact

2. **Remove Planhat from src/components/ToolsMarquee.jsx** (BUG-02):
   - Remove the `import planhat from "../assets/planhat-logo.svg";` import (line 2)
   - Remove the `{ src: planhat, alt: "Planhat" }` entry from the LOGOS array (line 16)
   - Keep all other imports and array entries intact

3. **Optionally delete the SVG file:**
   - Delete `src/assets/planhat-logo.svg` if no other file imports it (verify with grep first)

4. **Fix banner link** (BUG-01):
   - In `src/index.jsx`, the banner at lines 42-45 has `<a href="#">click here →</a>`
   - The user wants this to point to a real URL but the destination is TBD
   - For now, change the banner text and link to point to the Cal.com booking link (the site's primary CTA). Import CAL_LINK from data/content.js if not already imported, and use `https://cal.com/${CAL_LINK}` as the href. Update the text to: "Book a free automation consultation — <a href=...>schedule a call →</a>"
   - If CAL_LINK is already imported in index.jsx, reuse it. If not, import it.
   - This is a reasonable default since the banner should drive conversions. The user can change the URL later.
  </action>
  <verify>
    <automated>cd C:/Users/amin/WebstormProjects/website && ! grep -ri "planhat" src/data/content.js src/components/ToolsMarquee.jsx && ! grep 'href="#"' src/index.jsx && npm run build && echo "Planhat removed and banner fixed"</automated>
  </verify>
  <done>No "planhat" references in content.js or ToolsMarquee.jsx, banner link in index.jsx uses a real URL (not href="#"), build succeeds without errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify fonts render and banner link works</name>
  <what-built>Self-hosted fonts replacing Google Fonts CDN, Planhat logo removed from marquee, banner link updated to booking CTA</what-built>
  <files>none</files>
  <action>Human verifies fonts, logo removal, and banner link visually in browser</action>
  <verify>
    <automated>echo "checkpoint — human verification"</automated>
  </verify>
  <done>User confirms fonts render from self-hosted WOFF2, Planhat is gone, banner link works</done>
  <how-to-verify>
    1. Run `npm run dev` and open the site in your browser
    2. Open DevTools Network tab, filter by "font" — verify WOFF2 files load from localhost (not fonts.googleapis.com or fonts.gstatic.com)
    3. Verify text renders in DM Sans (body) and DM Mono (code sections) with no flash of unstyled text
    4. Scroll to the logo/tools marquee section — verify Planhat logo is no longer displayed
    5. Check the banner at the top — verify the link text and URL are sensible (should point to Cal.com booking)
    6. Click the banner link — verify it opens the booking page
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
After all tasks, verify the full set:
```bash
cd C:/Users/amin/WebstormProjects/website
npm run build
# Fonts
ls dist/assets/*.woff2 | wc -l  # Should be 5+ WOFF2 files
! grep -i "googleapis" dist/index.html && echo "No Google Fonts"
# Planhat
! grep -ri "planhat" src/data/content.js src/components/ToolsMarquee.jsx && echo "Planhat removed"
# Banner
! grep 'href="#"' src/index.jsx && echo "Banner link fixed"
```
</verification>

<success_criteria>
- WOFF2 font files are present in dist/assets/ after build
- Zero references to googleapis.com or gstatic.com in built HTML
- "Planhat" does not appear in content.js or ToolsMarquee.jsx
- Banner link in index.jsx uses a real URL, not href="#"
- Build completes without errors
- User confirms fonts render correctly and Planhat is gone (checkpoint)
</success_criteria>

<output>
After completion, create `.planning/phases/01-seo-foundation-and-static-assets/01-02-SUMMARY.md`
</output>
