---
phase: 01-seo-foundation-and-static-assets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - public/robots.txt
  - public/sitemap.xml
  - public/og-image.png
autonomous: true
requirements:
  - SEO-01
  - SEO-02
  - SEO-03
  - SEO-04
  - SEO-05
  - PERF-06

must_haves:
  truths:
    - "Viewing page source shows complete meta tags (title, description, canonical) in the HTML head"
    - "Open Graph and Twitter Card meta tags are present with correct content values"
    - "A JSON-LD script block with Organization schema is present in the head"
    - "public/sitemap.xml and public/robots.txt exist with valid content"
    - "Favicon link resolves correctly after Vite build (no 404)"
    - "Cal.com preconnect hint is present in the head"
  artifacts:
    - path: "index.html"
      provides: "Complete SEO meta tags, OG tags, Twitter Card tags, JSON-LD, resource hints, favicon fix"
      contains: "og:title"
    - path: "public/robots.txt"
      provides: "Crawl directives with sitemap reference"
      contains: "Sitemap:"
    - path: "public/sitemap.xml"
      provides: "Single-page sitemap for search engines"
      contains: "urlset"
    - path: "public/og-image.png"
      provides: "1200x630 branded social sharing image"
  key_links:
    - from: "index.html"
      to: "public/og-image.png"
      via: "og:image meta tag with absolute URL"
      pattern: "og:image.*project-groovymango\\.github\\.io/website/og-image\\.png"
    - from: "public/robots.txt"
      to: "public/sitemap.xml"
      via: "Sitemap directive URL"
      pattern: "Sitemap:.*sitemap\\.xml"
---

<objective>
Add all SEO metadata, social sharing tags, structured data, crawl infrastructure files, and resource hints to the site's static HTML and public directory.

Purpose: Search engines and social platforms will see complete, accurate metadata when crawling or sharing the site URL, and the favicon will load without 404 on GitHub Pages.
Output: Updated index.html with full SEO head, plus robots.txt, sitemap.xml, and og-image.png in public/.
</objective>

<execution_context>
@C:/Users/amin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/amin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-seo-foundation-and-static-assets/01-CONTEXT.md
@.planning/phases/01-seo-foundation-and-static-assets/01-RESEARCH.md

<interfaces>
<!-- Current index.html head structure (to be replaced/augmented): -->
From index.html:
```html
<meta name="description" content="a2labs — automation consulting..." />
<link rel="icon" href="/frog.gif" type="image/gif" />
<title>a2labs — automation consulting</title>
<link rel="preconnect" href="https://fonts.googleapis.com" />
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
<link href="https://fonts.googleapis.com/css2?family=..." rel="stylesheet" />
```

From vite.config.js:
```javascript
base: "/website/"  // All asset paths prefixed with /website/
```

GitHub Pages URL: https://project-groovymango.github.io/website/
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SEO meta tags, OG tags, Twitter Cards, JSON-LD, and resource hints to index.html</name>
  <files>index.html</files>
  <action>
Replace the existing `<head>` content in index.html with a complete SEO-optimized head. Keep the charset, viewport, and Vite script tag. The Google Fonts links will be removed in Plan 02 (which handles font migration), so leave them for now -- Plan 02 owns that change.

Add the following in the `<head>`, after charset and viewport:

1. **Resource hints** (PERF-06):
   - `<link rel="preconnect" href="https://cal.com" crossorigin />`

2. **Basic SEO meta tags** (SEO-01):
   - `<title>A2 Labs — iPaaS Automation & Workflow Consulting</title>` (replaces existing title -- this is a locked user decision)
   - `<meta name="description" content="...">` -- write a keyword-optimized description ~150 chars including: iPaaS, Make.com, n8n, workflow automation, consulting, founders and teams. Claude's discretion on exact copy.
   - `<link rel="canonical" href="https://project-groovymango.github.io/website/" />`

3. **Open Graph tags** (SEO-02):
   - og:type = "website"
   - og:title = same as title tag
   - og:description = same as or variant of meta description
   - og:url = canonical URL
   - og:image = "https://project-groovymango.github.io/website/og-image.png"
   - og:image:width = "1200"
   - og:image:height = "630"
   - og:site_name = "A2 Labs"

4. **Twitter Card tags** (SEO-02):
   - twitter:card = "summary_large_image"
   - twitter:title = same as title
   - twitter:description = shortened version of description
   - twitter:image = same as og:image

5. **Favicon fix** (SEO-05):
   - Keep existing `<link rel="icon" href="/frog.gif" type="image/gif" />` -- Vite transforms the leading `/` path to include `/website/` base during build. Verify this in the build output.

6. **JSON-LD structured data** (SEO-03):
   - Add a `<script type="application/ld+json">` block before closing `</head>`
   - Schema: Organization ONLY (locked user decision -- not ProfessionalService, not FAQPage)
   - Fields: @context, @type, name ("A2 Labs"), url (canonical), logo (og-image URL), email ("hello@a2labs.io"), description, sameAs (empty array -- social URLs deferred to Phase 2)

Do NOT touch the `<body>` or Vite script tag. Do NOT remove Google Fonts links (Plan 02 handles that).
  </action>
  <verify>
    <automated>cd C:/Users/amin/WebstormProjects/website && npm run build && grep -c "og:title" dist/index.html && grep -c "twitter:card" dist/index.html && grep -c "application/ld+json" dist/index.html && grep -c "canonical" dist/index.html && grep "website/frog.gif" dist/index.html && grep -c "preconnect.*cal.com" dist/index.html</automated>
  </verify>
  <done>index.html contains: title with "iPaaS Automation", meta description with keywords, canonical URL, 8 OG tags, 4 Twitter Card tags, JSON-LD Organization block, Cal.com preconnect, and favicon path resolves to /website/frog.gif in built output</done>
</task>

<task type="auto">
  <name>Task 2: Create sitemap.xml, robots.txt, and OG image in public/</name>
  <files>public/robots.txt, public/sitemap.xml, public/og-image.png</files>
  <action>
Create three files in the `public/` directory:

1. **public/robots.txt** (SEO-04):
```
User-agent: *
Allow: /

Sitemap: https://project-groovymango.github.io/website/sitemap.xml
```

2. **public/sitemap.xml** (SEO-04):
```xml
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url>
    <loc>https://project-groovymango.github.io/website/</loc>
    <changefreq>monthly</changefreq>
    <priority>1.0</priority>
  </url>
</urlset>
```

3. **public/og-image.png** (for SEO-02):
   Create a simple branded OG image at exactly 1200x630 pixels. Use one of these approaches (in order of preference):
   - Use Node.js canvas (`npm exec -- canvas` or a quick script) to generate a PNG with: dark background (#0a0a0a or similar from the site's color scheme), "A2 Labs" text in white, tagline "iPaaS Automation & Workflow Consulting" in muted color, and a simple accent line or shape in the brand green (#34d399).
   - If canvas is not available, create a minimal valid PNG using a different programmatic approach.
   - The image must be a real raster image (PNG or JPG), NOT an SVG -- social platforms do not render SVG.
   - Target file size: under 200KB.
  </action>
  <verify>
    <automated>cd C:/Users/amin/WebstormProjects/website && npm run build && test -f dist/robots.txt && test -f dist/sitemap.xml && test -f dist/og-image.png && grep "Sitemap:" dist/robots.txt && grep "urlset" dist/sitemap.xml && echo "All crawl files present"</automated>
  </verify>
  <done>robots.txt exists with correct Sitemap URL, sitemap.xml has the canonical page URL, og-image.png exists as a valid raster image at 1200x630</done>
</task>

</tasks>

<verification>
After both tasks, run a full build and verify all SEO elements:
```bash
cd C:/Users/amin/WebstormProjects/website
npm run build
# Meta tags
grep "og:title" dist/index.html
grep "twitter:card" dist/index.html
grep "canonical" dist/index.html
grep "application/ld+json" dist/index.html
# Favicon
grep "website/frog.gif" dist/index.html
# Resource hints
grep "preconnect.*cal.com" dist/index.html
# Crawl files
test -f dist/robots.txt && echo "robots.txt OK"
test -f dist/sitemap.xml && echo "sitemap.xml OK"
test -f dist/og-image.png && echo "og-image.png OK"
```
</verification>

<success_criteria>
- Built dist/index.html contains complete SEO meta tags, OG tags, Twitter Card tags, JSON-LD Organization schema, canonical URL, and Cal.com preconnect
- dist/robots.txt and dist/sitemap.xml exist with correct absolute URLs including /website/ base path
- dist/og-image.png exists as a valid 1200x630 raster image
- Favicon path in built HTML includes /website/ prefix
- No Google Fonts links have been removed yet (that is Plan 02's responsibility)
</success_criteria>

<output>
After completion, create `.planning/phases/01-seo-foundation-and-static-assets/01-01-SUMMARY.md`
</output>
