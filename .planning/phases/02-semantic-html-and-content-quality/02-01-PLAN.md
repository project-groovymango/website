---
phase: 02-semantic-html-and-content-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.jsx
  - src/styles/global.css
  - src/data/content.js
autonomous: true
requirements:
  - HTML-01
  - HTML-02
  - SEO-06
must_haves:
  truths:
    - "DOM has exactly one h1 element ('a2labs') and all other headings are h2 or h3 with no level gaps"
    - "No span[role=heading] elements remain anywhere in the rendered page"
    - "Every h3 has a preceding h2 ancestor section (no h1-to-h3 jumps)"
    - "No href='#' placeholder links remain in the rendered page"
    - "Keywords Make.com, n8n, workflow automation, or HubSpot automation appear naturally in visible page copy"
    - "Visual appearance is identical before and after -- no layout shifts, font size changes, or spacing changes from heading tag swaps"
  artifacts:
    - path: "src/index.jsx"
      provides: "Semantic heading hierarchy and cleaned footer"
      contains: "h2 className"
    - path: "src/styles/global.css"
      provides: "CSS resets for heading elements"
      contains: "h2.section-label"
    - path: "src/data/content.js"
      provides: "Keyword-enhanced FAQ and case study copy"
  key_links:
    - from: "src/styles/global.css"
      to: "src/index.jsx"
      via: "heading class selectors must match new tag+class combinations"
      pattern: "h[23]\\.(section-label|case-title|step-title|team-name)"
---

<objective>
Replace heading hacks with proper semantic HTML (h2/h3), remove dead footer links, and weave target keywords into page copy — all while preserving the exact current visual appearance.

Purpose: Improve search engine ranking signals, accessibility, and crawl quality for the a2labs marketing site.
Output: Updated index.jsx with semantic headings and clean footer, updated global.css with heading resets, updated content.js with keyword-optimized copy.
</objective>

<execution_context>
@C:/Users/amin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/amin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-semantic-html-and-content-quality/02-CONTEXT.md
@.planning/phases/02-semantic-html-and-content-quality/02-RESEARCH.md

@src/index.jsx
@src/styles/global.css
@src/data/content.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Semantic heading hierarchy and CSS resets</name>
  <files>src/index.jsx, src/styles/global.css</files>
  <action>
Replace all span[role=heading] workarounds with native heading elements and add CSS resets to preserve visual appearance.

In src/index.jsx, make these exact tag replacements:

1. Line 94: Replace `<p className="section-label"><span role="heading" aria-level="2">Previous projects</span></p>` with `<h2 className="section-label">Previous projects</h2>`

2. Line 101: Replace `<div className="case-title">{title}</div>` with `<h3 className="case-title">{title}</h3>` (inside visibleCases.map)

3. Line 112: Replace `<div className="case-title">{title}</div>` with `<h3 className="case-title">{title}</h3>` (inside hiddenCases.map)

4. Line 132: Replace `<p className="section-label"><span role="heading" aria-level="2">How it works</span></p>` with `<h2 className="section-label">How it works</h2>`

5. Lines 149, 156, 163 (audit track): Replace `<div className="step-title">...</div>` with `<h3 className="step-title">...</h3>` for all three step titles

6. Lines 173, 180, 187 (problem track): Replace `<div className="step-title">...</div>` with `<h3 className="step-title">...</h3>` for all three step titles

7. Team section (~line 201-216): Add an `<h2 className="section-label">The team</h2>` BEFORE the `<div className="team-list">` element. This prevents an h1-to-h3 gap.

8. Line 209: Replace `<div className="team-name">Amin Laanaya</div>` with `<h3 className="team-name">Amin Laanaya</h3>`

9. Line 220: Replace `<p className="section-label"><span role="heading" aria-level="2">FAQ</span></p>` with `<h2 className="section-label">FAQ</h2>`

In src/styles/global.css, update selectors to be tag-specific and add margin resets:

1. Change the existing `.section-label` rule (line 35) to target `h2.section-label` — the existing properties (font-size: 14px, font-weight: 500, text-transform, letter-spacing, color, margin-bottom: 16px) already override browser defaults EXCEPT margin-top. Add `margin-top: 0;` explicitly or keep `margin-bottom: 16px` and add `margin: 0 0 16px 0` to be safe.

2. The existing `.case-title` rule (line 105) already sets font-size: 14px, font-weight: 600, color. Add `margin: 0;` to it to override browser h3 default margins. Update selector to `h3.case-title, .case-title` (keep backward compat for any non-heading usage).

3. The existing `.step-title` rule (line 40) sets font-size, font-weight, color, margin-bottom, cursor. Add `margin-top: 0;` to override browser h3 margin. Update to ensure margin is explicit: `margin: 0 0 4px 0;`.

4. The existing `.team-name` rule (line 122) sets font-size, font-weight, color, letter-spacing. Add `margin: 0;` to it.

IMPORTANT: Do NOT change the visual appearance. The goal is tag-only changes. After these changes, the page should look pixel-identical. Verify by running `npx vite build` to ensure no syntax errors.
  </action>
  <verify>
    <automated>cd C:/Users/amin/WebstormProjects/website && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - All three section labels use h2 tags (no span[role=heading] remains)
    - All case-title, step-title, and team-name elements use h3 tags
    - Team section has an h2 before the h3
    - CSS resets prevent any visual change from tag swaps
    - Build succeeds without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Footer placeholder link cleanup</name>
  <files>src/index.jsx, src/styles/global.css</files>
  <action>
Remove all dead href="#" links from the footer. Per research recommendation: remove the "Resources" and "Company" columns entirely since all their links are dead, keep only the "Inquiries" column with email and CTA.

In src/index.jsx footer (lines 240-276):

1. Remove the entire first footer column (Resources): the div containing "Resources" heading, "Terms" link, "Privacy Policy" link, and all four social icon links (Slack, LinkedIn, Discord, Telegram). This is the `<div className="a2-footer-col">` block from ~line 243 to ~line 253.

2. Remove the entire second footer column (Company): the div containing "Company" heading, "About" link, and "Careers" link. This is the `<div className="a2-footer-col">` block from ~line 254 to ~line 258.

3. Keep the third footer column (Inquiries) with email and CTA button unchanged.

4. Keep the footer frog image and copyright line unchanged.

After removal, the footer grid will have just the Inquiries column and the frog. Adjust the layout: since there is only one column now, remove the `<div className="a2-footer-cols">` wrapper and place the Inquiries column directly inside `a2-footer-grid`, OR keep the wrapper with just one column inside — whichever results in cleaner JSX.

In src/styles/global.css: No CSS changes needed — the remaining elements keep their existing classes. The removed columns' CSS rules (.a2-footer-col rules) are still used by the remaining Inquiries column. Leave them as-is (dead CSS cleanup is out of scope).

IMPORTANT: The footer must still look clean and not broken. The frog should still appear on the right side.
  </action>
  <verify>
    <automated>cd C:/Users/amin/WebstormProjects/website && npx vite build && grep -c 'href="#"' dist/assets/*.js</automated>
  </verify>
  <done>
    - Zero href="#" links remain in the built output
    - Footer shows only Inquiries column (email + CTA) and frog image
    - Footer copyright line still present
    - Build succeeds
  </done>
</task>

<task type="auto">
  <name>Task 3: Keyword optimization in headings and copy</name>
  <files>src/index.jsx, src/data/content.js</files>
  <action>
Weave target keywords (Make.com, n8n, workflow automation, HubSpot automation) naturally into visible page copy. Per user decision: keep the playful, casual tone. Do NOT make it corporate. "iPaaS" is NOT a target keyword. Keywords should appear 3-5 times total across the page in natural contexts.

Priority keyword placements (Claude's discretion on exact wording):

1. **Tagline (src/index.jsx, line 61):** The current tagline "Automate, automate, automate -- we're nerds who get high on things running efficiently. Help us get our fix, please." Naturally include "workflow automation" if it fits the tone. Example: "Workflow automation nerds who get high on things running efficiently." — but keep it casual and funny. Do NOT force it if it sounds stiff.

2. **Description paragraph (src/index.jsx, line 63):** This long paragraph about automation is a natural place to mention specific tools. Weave in "Make.com" or "n8n" or "workflow automation" where it makes sense. Keep the self-deprecating tone.

3. **Work intro (src/index.jsx, line 95):** "...if it has an API, we can help you automate or rethink the process." Could naturally reference "workflow automation" or specific tools.

4. **Section headings:** Per user decision, keep current text UNLESS keywords fit naturally. "How it works" should stay as-is (adding "workflow automation" to it would feel forced). "Previous projects" could potentially become "Automation projects" but only if it feels natural.

5. **FAQ answers (src/data/content.js):** FAQ answers already mention automation tools organically. Enhance where natural:
   - "What do you do in a nutshell?" answer could mention Make.com or n8n
   - "How do you make the automations?" answer could explicitly name Make.com and n8n as the primary platforms
   - "In which CRMs do we have experience in?" answer already mentions HubSpot — ensure "HubSpot automation" appears as a phrase

6. **Case study descriptions (src/data/content.js):** These are factual project summaries. Add tool references only if contextually accurate (e.g., "Built using Make.com" or "HubSpot automation workflow"). Do not fabricate tool usage.

CONSTRAINTS:
- Do NOT use "iPaaS" as a keyword (user explicitly de-selected it)
- Keep the playful, self-deprecating brand voice throughout
- 3-5 natural keyword mentions across the entire page — not in every paragraph
- h1 stays as "a2labs" — no keywords in h1
- Section h2 headings: keep current text unless a keyword fits naturally without sounding corporate

After changes, run build to verify no syntax errors.
  </action>
  <verify>
    <automated>cd C:/Users/amin/WebstormProjects/website && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Keywords (Make.com, n8n, workflow automation, HubSpot automation) appear 3-5 times in visible page copy
    - "iPaaS" does NOT appear as a keyword in any new or modified text
    - Tone remains casual, playful, and self-deprecating — not corporate
    - Build succeeds
    - h1 is still "a2labs" with no keywords added
  </done>
</task>

</tasks>

<verification>
After all three tasks:

1. Build succeeds: `npx vite build` exits 0
2. No heading hacks remain: `grep -c 'role="heading"' src/index.jsx` returns 0
3. No dead links remain: `grep -c 'href="#"' src/index.jsx` returns 0
4. Heading hierarchy is correct: Single h1, followed by h2s, each h2 has h3s under it, no level gaps
5. Keywords present: grep for "Make.com" OR "n8n" OR "workflow automation" in built JS shows matches
6. Visual appearance unchanged (heading sections look identical — verified by dev server inspection)
</verification>

<success_criteria>
- DOM shows exactly one h1 ("a2labs"), four h2s ("Previous projects", "How it works", "The team", "FAQ"), and multiple h3s (case titles, step titles, team name)
- Zero span[role=heading] elements in the codebase
- Zero href="#" links in the codebase
- Target keywords appear naturally 3-5 times in visible copy
- Page visual appearance is unchanged
- `npx vite build` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/02-semantic-html-and-content-quality/02-01-SUMMARY.md`
</output>
